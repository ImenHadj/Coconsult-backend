pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                git branch: 'principal', url: 'https://github.com/ImenHadj/Coconsult-backend.git'        }
    }
}
}
